Finish basic function.
Run sample code `testfile.c`:
    make
    make test
It will display the diff result with result.txt

Run bonus code `bonus.c`:
    make
    make bonus
It will display the diff result with result-bonus.txt
